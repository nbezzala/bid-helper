<%= form_for(@task_seeker) do |f| %>
  <% if @task_seeker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_seeker.errors.count, "error") %> prohibited this task_seeker from being saved:</h2>

      <ul>
      <% @task_seeker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
